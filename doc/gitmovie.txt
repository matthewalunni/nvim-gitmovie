GitMovie - Neovim plugin to replay git commits as animated diffs

Commands
  - GitMovie: Open gitmovie in the current directory

Usage
  1) TBA

Notes
  - The plugin renders a window containing the animated diff for each commit, line-by-line.
  - Diff lines are colored: additions in green, deletions in red, context in gray.
